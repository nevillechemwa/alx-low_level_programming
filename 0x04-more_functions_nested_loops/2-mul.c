#include "main.h"

/**
 * mul - Multiplies two int
 * @a: The first number to multiply
 * @b: The second number to multiply with the first number
 *
 * Return: The result of multiplication between a pamam and b param
 */
int mul(int a, int b)
{
	return (a * b);
}
